#!/usr/bin/env node

'use strict';

var chalk = require('chalk');
var copy = require('copy-paste').copy;

var ipv6 = require('..');

copy(ipv6, function() {
  console.log('Your ipv6 is: '+ chalk.cyan(ipv6) +' which was in your clipboard.');
});
